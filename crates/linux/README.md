# sandy-linux

Private Linux enforcement backend for Sandy. It lowers a validated,
platform-neutral policy into a fixed native plan, pins every ambient resource,
and applies the irreversible boundary to the calling process.

This package is an implementation detail. Use the supported `sandy-sandbox`
facade instead of depending on it directly.
